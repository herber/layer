<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%= name %></title>
        <link rel="stylesheet" href="main.css">
        <style media="screen">
            hr:last-child {
              display: none;
            }
        </style>
    </head>
    <body>
        <div class="main">
            <h1><a href="/"><%= name %></a></h1>
            <div class="posts">
                <% for(var i = 0; i < posts.length; i++){ %>
                <div class="post">
                    <h3><a href="/<%= posts[i].url %>"><%= posts[i].title %></a></h3>
                    <i>by <a href="~<%= posts[i].userurl %>"><%= posts[i].user %></a>. <%= posts[i].date %></i>
                    <p><% for(var x = 0; x < posts[i].text.length && x < 600; x++){ %><%- posts[i].text[x] %><% } %></p>
                </div>
                <hr>
                <% } %>
            </div>
        </div>
    </body>
</html>
