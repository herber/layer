<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%= name %></title>
        <link rel="stylesheet" href="../dashboard.css">
        <style media="screen">
            hr:last-child {
              display: none;
            }

            .main {
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="main">
            <h1><a href="/"><%= name %></a></h1>
            <form action="/dashboard/login/now/" method="post">
                <h3>Login</h3>
                <input class="input" type="text" name="username" value="Username" pattern="[^'\x22]+" title="Invalid input" required />
                <input class="input" type="password" name="password" value="Password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters" required />
                <button type="submit">Submit</button>
            </form>
        </div>
    </body>
</html>
